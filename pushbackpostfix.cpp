#include <bits/stdc++.h>

using namespace std;

vector <string> v[1010];
char input[1010];

int main(){

    for(int i = 0 ; i < 8 ; i++){
    scanf(" %s", &input);
    v[i].push_back(input);
    if(input >= 'A' && input <= 'Z'){

    }
    }



    return 0;
}

